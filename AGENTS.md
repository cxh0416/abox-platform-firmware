# 文档边界

- `docs/protocols/` 中的对外协议只定义身份、Topic、字段、命令、响应码、QoS 和必要的接口语义。
- 对外协议不得记录项目进度、发布版本或提交号、源码实现状态、内部控制算法、硬件映射和测试验收记录。
- 设备内部实现、运维接口和验证记录必须保存在明确标识为内部的实现、运维或验收文档中，不得混入对外交付材料。

# 本地构建目录

- 所有本仓库编译缓存统一放在仓库根目录的 `build/` 下。
- 主机测试复用 `build/host/`；公共 Boot 复用 `build/boot/Debug/` 或 `build/boot/Release/`。
- 不按任务、阶段或尝试次数新建 `build-*`、`out/` 等平级目录；确需区分工具链时，在 `build/` 下使用固定名称并复用。
